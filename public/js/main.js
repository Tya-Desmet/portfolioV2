// Main JavaScript - Minimal interactions for v0.1
// Most functionality will be added in later stories (navigation, forms, etc.)

document.addEventListener('DOMContentLoaded', () => {
    console.log('Mystya Portfolio - v0.1 loaded');
    
    // Smooth scroll behavior is handled by CSS (scroll-behavior: smooth)
    // This is a placeholder for future JavaScript functionality
});
